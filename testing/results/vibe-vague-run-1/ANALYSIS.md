## Intent Analysis

- **Matched scaffold:** ❌ Cannot determine — no domain keywords detected
- **Inferred language:** Python (FastAPI) — no language clues, using default
- **Inferred scale:** ~100K users, ~10M documents — no scale clues, using default

⚠️ **Cannot proceed with generation.** The description "I want to build an app with a database" doesn't contain enough information to determine query patterns, which are required for partition key selection.

**Clarifying question:**
"When users open your app, what's the first thing they see? That tells me how to optimize the data layout."

*Generation paused — awaiting user response.*
